import React from "react";
import ReactDOM from "react-dom/client";
import BridgeZeroApp from "./BridgeZeroApp";
import "./bridge.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BridgeZeroApp />
  </React.StrictMode>
);
