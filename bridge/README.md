# Bridge Interfaces

Bridge interfaces are the operational faces of OMEGA-ARC.

- `bridge-zero/`: Engineering command bridge for architecture development.
- `shared/`: Shared utilities and assets used by multiple bridge interfaces.

Future interfaces can be added as siblings (for example Bridge One and Bridge Two) without renaming Bridge Zero.
